{
  "name": "intellia237-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "powershell -NoProfile -Command \"if (Test-Path lib) { Remove-Item lib -Recurse -Force }\"",
    "test": "vitest run",
    "test:rules": "npm run test:rules:firestore && npm run test:rules:storage && npm run test:rules:assets",
    "test:rules:firestore": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore \"vitest run --config vitest.rules.config.ts src/__tests__/rules/firestore.rules.test.ts\"",
    "test:rules:storage": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore,storage \"vitest run --config vitest.rules.config.ts src/__tests__/rules/storage.rules.test.ts\"",
    "test:integration:family-access": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore,auth \"vitest run --config vitest.rules.config.ts --no-file-parallelism src/__tests__/rules/familyAccess.integration.test.ts src/__tests__/rules/familyBilling.integration.test.ts\"",
    "test:integration:study-reserve": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore \"vitest run --config vitest.rules.config.ts src/__tests__/rules/studyReserve.integration.test.ts\"",
    "test:integration:announcements": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore \"vitest run --config vitest.rules.config.ts src/__tests__/rules/announcementFanout.integration.test.ts\"",
    "test:integration:account-deletion": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore \"vitest run --config vitest.rules.config.ts src/__tests__/rules/accountDeletion.integration.test.ts\"",
    "test:integration:parcours": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore \"vitest run --config vitest.rules.config.ts src/__tests__/rules/flowFeed.integration.test.ts\"",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --config ..\\firebase.json --only auth,firestore,functions,storage --project demo-intellia237",
    "pregenerate": "tsx scripts/pregenerate.ts",
    "test:rules:assets": "firebase emulators:exec --config ../firebase.json --project demo-intellia237 --only firestore,storage \"vitest run --config vitest.rules.config.ts src/__tests__/rules/educational-assets.rules.test.ts\""
  },
  "dependencies": {
    "axios": "^1.15.0",
    "dotenv": "^17.4.1",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.0.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^20.19.43",
    "firebase": "^12.15.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.1.1"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
