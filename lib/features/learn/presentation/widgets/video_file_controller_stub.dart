import 'package:video_player/video_player.dart';

VideoPlayerController videoFileController(String path) =>
    throw UnsupportedError(
      'La source locale nécessite un appareil compatible.',
    );
